Se genera una clave aleatoria del mismo tamaño que el texto.

Cada carácter se convierte a número ASCI y se aplica XOR con la clave.

Aplicando XOR otra vez con la misma clave, se obtiene el texto original.
